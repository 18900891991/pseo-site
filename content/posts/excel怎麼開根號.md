---
title: "excel怎麼開根號"
date: 2026-02-07T08:17:23.656515+08:00
description: "Excel 怎麼開根號？  在 Excel 裡做數學運算時，開根號（求平方根）是最常見的需求之一。無論是統計分析、工程計算，還是資料處理，熟練掌握開根號的技巧都能大幅提升工作效率。  ---   原因分析  | 為什麼要在 Excel 開根號？ | 典型場景 | |-------------"
draft: false
categories: ["AI教程"]
---

# Excel 怎麼開根號？

在 Excel 裡做數學運算時，開根號（求平方根）是最常見的需求之一。無論是統計分析、工程計算，還是資料處理，熟練掌握開根號的技巧都能大幅提升工作效率。

---

## 原因分析

| 為什麼要在 Excel 開根號？ | 典型場景 |
|--------------------------|----------|
| **數據處理** | 計算距離、標準差、機率等需要平方根 |
| **數學模型** | 求解方程、做投資回報率計算 |
| **資料清洗** | 去除負數、處理極值後再做平方根 |
| **可視化** | 在圖表中顯示根號數值 |
| **自動化** | 用公式批量處理大量數據 |

掌握三種常見的開根號方法，能在不同情境下選擇最合適的解決方案。

---

## 方案一：使用 `SQRT` 函數

`SQRT(number)` 直接返回給定數字的平方根。

### 步驟

1. **選擇目標單元格**  
   在你想要顯示根號結果的單元格輸入公式。

2. **輸入公式**  
   ```excel
   =SQRT(A1)
   ```
   其中 `A1` 是你想開根號的數值所在的單元格。

3. **確認輸出**  
   按 `Enter`，單元格將顯示平方根值。

### 範例

| A | B |
|---|---|
| 16 | `=SQRT(A1)` → 4 |
| 25 | `=SQRT(A1)` → 5 |

> **注意**：`SQRT` 只能處理非負數，若輸入負數會得到 `#NUM!` 錯誤。

---

## 方案二：使用 `POWER` 函數

`POWER(number, power)` 可以用來做更廣泛的指數運算，平方根相當於 `POWER(number, 0.5)`。

### 步驟

1. **選擇目標單元格**  
   在你想要顯示根號結果的單元格輸入公式。

2. **輸入公式**  
   ```excel
   =POWER(A1, 0.5)
   ```
   或者 `=POWER(A1,1/2)`。

3. **確認輸出**  
   按 `Enter`，單元格將顯示平方根值。

### 範例

| A | B |
|---|---|
| 9 | `=POWER(A1,0.5)` → 3 |
| 49 | `=POWER(A1,1/2)` → 7 |

> **優點**：可靈活改為立方根（`POWER(A1,1/3)`）或其他根號。

---

## 方案三：使用 `CHAR` 或 `TEXT` 產生根號符號

如果你想在工作表中顯示 **根號符號**（√）而不是數值，或者需要將根號符號與數字組合成文字。

### 步驟

1. **顯示根號符號**  
   在單元格輸入 `=CHAR(8730)` 或直接輸入 `√`（在鍵盤上輸入 Alt+251 或使用符號插入）。

2. **組合文字**  
   ```excel
   ="√"&A1
   ```
   或者
   ```excel
   ="√"&TEXT(A1,"0.00")
   ```

3. **使用公式計算**  
   若要同時顯示根號符號和計算結果，可結合 `SQRT`：
   ```excel
   ="√"&A1&" = "&SQRT(A1)
   ```

### 範例

| A | B | C |
|---|---|---|
| 36 | `=CHAR(8730)` → √ | `="√"&A1&" = "&SQRT(A1)` → √36 = 6 |

> **提示**：使用 `CHAR(8730)` 可在不輸入特殊符號的情況下產生根號。

---

## 常見問題解答（FAQ）

| 問題 | 解答 |
|------|------|
| **Q1：輸入負數會出現什麼錯誤？** | `SQRT` 與 `POWER` 只能處理非負數，負數會返回 `#NUM!`。若需處理複數，可使用 `IMABS` 或 `COMPLEX`。 |
| **Q2：如何一次性開多個根號？** | 在列或行中拖拉公式，或使用填充功能。也可使用 `ARRAYFORMULA`（在 Google Sheets）或 `=SQRT(A1:A10)`（在 Office 365）。 |
| **Q3：根號符號顯示不出來，怎麼辦？** | 確認單元格格式為「常規」或「文字」。若仍不顯示，使用 `=CHAR(8730)` 或直接複製粘貼符號。 |
| **Q4：公式結果有誤差？** | Excel 的浮點數精度有限，特別是大數或多次運算時可做四捨五入：`=ROUND(SQRT(A1),2)`。 |
| **Q5：如何用 VBA 取根號？** | ```vba<br>Function SquareRoot(x As Double) As Double<br>    SquareRoot = x ^ 0.5<br>End Function<br>``` |

---

## 小結

- **最簡單**：直接使用 `SQRT` 函數。  
- **更彈性**：使用 `POWER` 可計算任意根。  
- **美化輸出**：用 `CHAR(8730)` 或文字拼接顯示根號符號。  

只要掌握上述三種方法，你就能在任何 Excel 任務中快速且正確地進行平方根計算。祝你開根號愉快！