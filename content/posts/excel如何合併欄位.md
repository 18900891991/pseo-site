---
title: "excel如何合併欄位"
date: 2026-02-07T08:17:23.529391+08:00
description: "Excel 如何合併欄位（合併單元格）全攻略   在日常工作中，常需要把多個相鄰的單元格合併為一個大單元格，以便做表頭、分組、佈局等。    這份教程將從 原因分析 出發，提供 三種分步驟的解決方案，並整理常見問題的解答，讓你輕鬆掌握 Excel 合併欄位的技巧。   1"
draft: false
categories: ["AI教程"]
---

# Excel 如何合併欄位（合併單元格）全攻略

> 在日常工作中，常需要把多個相鄰的單元格合併為一個大單元格，以便做表頭、分組、佈局等。  
> 這份教程將從 **原因分析** 出發，提供 **三種分步驟的解決方案**，並整理常見問題的解答，讓你輕鬆掌握 Excel 合併欄位的技巧。

## 1️⃣ 為什麼要合併欄位？

| 目的 | 典型場景 | 需要注意的點 |
|------|----------|--------------|
| **美化表格** | 表頭跨多列、報表標題 | 合併後不宜再做排序或篩選 |
| **分組顯示** | 章節分隔、分類標題 | 合併後單元格只能保留左上角的資料 |
| **佈局設計** | 佈滿空白區域、標準化格式 | 合併會影響公式計算、列寬調整 |
| **資料輸入** | 需要在同一格輸入多行文字 | 輸入時需使用 Alt+Enter 分行 |

> **小貼士**：合併單元格雖能改進視覺效果，但會破壞資料表的「純數據」結構，請在確定不需要排序/篩選時再使用。

---

## 2️⃣ 三種分步驟的解決方案

### 方案一：使用 Excel 功能區（GUI）合併

1. **選取需要合併的單元格**  
   - 點擊並拖動，或按住 `Ctrl` 選取不連續範圍。  
2. **點擊「開始」>「合併與置中」**  
   - 常見選項：  
     - **合併並置中**（Merge & Center）  
     - **合併不置中**（Merge）  
     - **取消合併**（Unmerge）  
3. **確認合併結果**  
   - 右上角只留一個儲存格，內容位於左上角。  
4. **若需要合併後仍保留多個資料**，可以在合併前使用 `Ctrl+C` 先備份，或在合併後再填寫新資料。

> **示例**  
> ![Merge Cells Screenshot](https://i.imgur.com/yourimage.png)

---

### 方案二：使用快捷鍵（鍵盤操作）合併

| 快捷鍵 | 功能 | 操作步驟 |
|--------|------|----------|
| `Alt + H + M + M` | 合併並置中 | 選取範圍後，按 `Alt` 進入功能區，然後 `H` → `M` → `M` |
| `Alt + H + M + C` | 合併不置中 | `Alt` → `H` → `M` → `C` |
| `Alt + H + M + U` | 取消合併 | `Alt` → `H` → `M` → `U` |

> **步驟**  
> 1. 選取範圍。  
> 2. 按 `Alt`，看到快捷鍵提示。  
> 3. 按相對應字母完成操作。  

> **優點**：省時，尤其在大量操作時更高效。

---

### 方案三：使用公式或 VBA 進行「合併」資料（不改變儲存格結構）

> **情境**：你不想真正合併儲存格，但想把多個儲存格的文字合併在一起顯示。

#### 3.1 公式合併（`TEXTJOIN` 或 `CONCATENATE`）

| 公式 | 說明 | 範例 |
|------|------|------|
| `=TEXTJOIN(" ",TRUE,A1,B1,C1)` | 以空格連接多個儲存格，忽略空白 | 把 A1、B1、C1 合併為一行文字 |
| `=CONCAT(A1,B1,C1)` | 直接連接 | 只顯示文字，無分隔符 |

> **步驟**  
> 1. 在目標儲存格輸入公式。  
> 2. 按 `Enter`，即時顯示合併結果。  
> 3. 若需要換行，使用 `CHAR(10)` 或 `&CHAR(10)&` 並勾選「自動換行」。

#### 3.2 VBA 合併（批次操作）

```vba
Sub MergeRange()
    Dim rng As Range
    Set rng = Range("A1:C1")  ' 需要合併的範圍
    rng.Merge
    rng.HorizontalAlignment = xlCenter
    rng.VerticalAlignment = xlCenter
End Sub
```

> **使用**：按 `Alt + F11` 開啟 VBA 編輯器 → 插入模組 → 複製貼上 → 執行 `MergeRange`。  

> **注意**：VBA 合併與 GUI 合併效果相同，但可批次處理多個範圍。

---

## 3️⃣ 常見問題解答（FAQ）

| 問題 | 解答 |
|------|------|
| **Q1：合併後資料排序會失敗嗎？** | 合併的單元格會被視為一個儲存格，排序時只以左上角的資料作為參考，若要排序，先取消合併或使用「分列」功能。 |
| **Q2：合併單元格後如何保留原有資料？** | 合併時，只保留左上角儲存格內容。若需要保留其他內容，可先將其複製到其他儲存格，或在合併後使用公式合併文字。 |
| **Q3：合併後單元格無法編輯？** | 合併後，只有一個可編輯的儲存格。若需逐個編輯，先取消合併。 |
| **Q4：合併會破壞公式嗎？** | 合併儲存格會改變公式參照方式，可能導致錯誤。建議只在報表或輸出中合併，避免在計算表中使用。 |
| **Q5：如何在合併後設置多行文字？** | 在儲存格內按 `Alt+Enter` 插入換行，並勾選「自動換行」使文字居中。 |
| **Q6：合併後儲存格大小會變嗎？** | 合併後會繼承第一行/第一列的大小，若需調整列寬/行高，可在合併後再調整。 |
| **Q7：能否合併不相鄰的單元格？** | Excel 允許合併相鄰或不相鄰的範圍，但不相鄰會被視為多個獨立合併，效果較不直觀。 |
| **Q8：合併後再拆分會失去資料嗎？** | 只會保留左上角的資料；其他儲存格的內容在合併前已被覆蓋，拆分後會丟失。 |

---

## 4️⃣ 小結

- **合併單元格** 主要用於視覺佈局和標題展示，**不宜在需要排序/篩選的大型資料表中使用**。  
- **方案一**：最直觀的功能區操作；  
- **方案二**：鍵盤快捷鍵，速度更快；  
- **方案三**：公式或 VBA，保留資料而不改變單元格結構。  

**記住**：在合併前先備份資料，或先在空白區域試驗，避免不可逆的錯誤。祝你在 Excel 中玩得開心，資料整理更高效！